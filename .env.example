# Optional: override home hero <video src> (HTTPS URL to an MP4/H.264, etc.).
# Leave unset to use /videos/0515.mov from the repo; Vercel runs `git lfs pull` before build (vercel.json).
# NORTHOPS_HERO_VIDEO_URL=https://cdn.example.com/northops-hero.mp4
