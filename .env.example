# Optional: override home hero <video src>.
# On Vercel, default is the repo file via GitHub media CDN (see route.ts).
# Local dev uses /videos/0515.mov from public/.
# NORTHOPS_HERO_VIDEO_URL=https://cdn.example.com/northops-hero.mp4
