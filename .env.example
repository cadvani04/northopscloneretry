# Production hero background (optional).
# If set, the home page <video> uses this URL instead of /videos/0515.mov.
# Vercel Hobby static assets are capped around 100MB; large Git LFS videos often
# do not ship with the deployment—use a CDN URL (MP4 H.264 + faststart recommended).
# NORTHOPS_HERO_VIDEO_URL=https://your-bucket.example.com/northops/hero.mp4
