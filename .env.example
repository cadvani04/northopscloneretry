# Optional: override hero <video src> on the home page (any public HTTPS media URL).
# If unset on Vercel, the app uses your shared Google Drive file instead of /videos/0515.mov
# (local dev still uses public/videos/0515.mov when VERCEL is not set).
# NORTHOPS_HERO_VIDEO_URL=https://cdn.example.com/northops-hero.mp4
